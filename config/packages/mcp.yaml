parameters: { }

mcp:
  app: 'tre_emme_backend'
  version: '1.0.0'
  client_transports:
    http: true
  http:
    # Espone l'endpoint MCP del bundle su /mcp
    path: '/mcp'
    session:
      store: 'framework'
      cache_pool: 'cache.app'
      ttl: 3600
