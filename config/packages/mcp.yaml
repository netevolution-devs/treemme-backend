mcp:
  client_transports:
    http: true
    stdio: false

  http:
    path: "/_mcp"
    session:
      store: "doctrine"
      connection: "default" # Usa la tua connessione database standard
      ttl: 3600